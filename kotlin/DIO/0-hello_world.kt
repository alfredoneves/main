package me.dio.hello_world	// divides the source code in packets for security and organization

fun main() {	// entry point of kotlin code
	val message = "Hello World"	// declares variable
	print("$message")		// prints variable without breaking line
	println("!")	// prints breaking line at the end
}
